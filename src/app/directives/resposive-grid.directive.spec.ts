import { ResposiveGridDirective } from './resposive-grid.directive';

describe('ResposiveGridDirective', () => {
  it('should create an instance', () => {
    const directive = new ResposiveGridDirective();
    expect(directive).toBeTruthy();
  });
});
